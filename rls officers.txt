-- Enable RLS on officers if not already
alter table public.officers enable row level security;

-- Allow public read access (for anon key)
create policy "Public read access"
on public.officers
for select
to anon
using (true);
